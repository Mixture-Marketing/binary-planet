{
  "name": "@mixturemarketing/mm-starter",
  "version": "0.0.1",
  "private": true,
  "description": "MixtureMarketing Astro 5 + CF Workers + Tailwind v4 template for client sites",
  "type": "module",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "build": "astro build",
    "dev": "astro dev",
    "preview": "wrangler dev",
    "deploy": "astro build && wrangler deploy",
    "check": "astro check",
    "typecheck": "astro check",
    "lint": "eslint src",
    "test": "node -e \"console.log('mm-starter: no unit tests yet (Astro template) — covered by build')\""
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.5.0",
    "@mixturemarketing/logger": "workspace:*",
    "@mixturemarketing/web-core": "workspace:*",
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^6.0.0",
    "tailwindcss": "^4.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@cloudflare/workers-types": "^4.20260520.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.93.1"
  }
}
